{
  "name": "codes-server",
  "version": "1.0.0",
  "description": "Simple Express server to serve static site and handle login",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
